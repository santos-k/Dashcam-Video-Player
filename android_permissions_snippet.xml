<!-- android/app/src/main/AndroidManifest.xml  (merge into your existing file) -->

<!--
  Add these permissions inside <manifest> before <application>:
-->

<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
    android:maxSdkVersion="32"/>
<uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
    android:maxSdkVersion="28"/>

<!--
  Inside <application>, add the FileProvider for sharing exported files:
-->

<provider
    android:name="androidx.core.content.FileProvider"
    android:authorities="${applicationId}.fileprovider"
    android:exported="false"
    android:grantUriPermissions="true">
  <meta-data
      android:name="android.support.FILE_PROVIDER_PATHS"
      android:resource="@xml/file_paths"/>
</provider>

<!--
  Also set android:requestLegacyExternalStorage="true" on <application>
  for Android 10 (API 29) compatibility:

  <application
      ...
      android:requestLegacyExternalStorage="true"
  >
-->
